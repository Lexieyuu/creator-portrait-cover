interface:
  display_name: "Creator Portrait Cover（自媒体活人感封面）"
  short_description: "Create lifelike 3:4 creator portrait covers"
  default_prompt: "Use $creator-portrait-cover as an image-editing composite: move the exact uploaded person into a lived-in background, never redraw or regenerate the person, target about 75% frame height without forcing the crop, and use no desktop scene or text."
policy:
  allow_implicit_invocation: true
