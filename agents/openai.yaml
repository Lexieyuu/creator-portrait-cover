interface:
  display_name: "Creator Portrait Cover（自媒体活人感封面）"
  short_description: "Create lifelike 3:4 creator portrait covers"
  default_prompt: "Use $creator-portrait-cover in background-only edit mode: protect and move the exact uploaded person into a simple lived-in environment, never regenerate the person, never add a foreground surface or desktop props, and output no text."
policy:
  allow_implicit_invocation: true
